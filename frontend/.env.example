# 프론트엔드 환경변수 예시 파일 (.env.local 또는 CI 환경변수로 사용)
# Vite는 빌드 시 VITE_ 접두사 변수만 코드에 주입합니다.

# 백엔드 API 주소. 미설정 시 http://localhost:8080 (로컬 개발 기본값).
# 운영(AWS) 빌드 시 실제 백엔드 주소로 지정하세요.
# VITE_API_BASE_URL=http://<EC2-퍼블릭-IP-또는-도메인>:8080
